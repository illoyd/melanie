- object = object.last if object.is_a?(Array)
- if object.try!(:errors).try!(:any?)
  #error_explanation
    %h2
      = pluralize(object.errors.count, "error")
      prevented us from saving
    %ul
      - object.errors.full_messages.each do |msg|
        %li= msg